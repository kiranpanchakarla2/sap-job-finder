import { JobDetailsPage } from "@/features/candidate-jobs";

/** Authenticated candidate job details — reuses public JobDetailsPage UI. */
export default function CandidateJobDetailsPage() {
  return <JobDetailsPage />;
}
