import { BulkJobUploadPage } from "@/features/employer-jobs";

export const metadata = {
  title: "Bulk Upload Jobs | Employer Portal | SAP Jobs Finder",
  description: "Upload multiple SAP job openings using our standard Excel template.",
};

export default function Page() {
  return <BulkJobUploadPage />;
}
