/**
 * Sprint 5A mock shortlisted candidates — unused at runtime after Sprint 5B.
 * Shortlisted candidates now load from job_applications via applicationService.
 */
export const mockShortlistedCandidates = [];
