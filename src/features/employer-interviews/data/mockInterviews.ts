/**
 * Sprint 5A mock interviews — unused at runtime after Sprint 5B.
 * Kept as an empty stub so old imports do not break.
 */
export function createMockInterviewsSeed() {
  return [];
}

export const mockInterviewsSeed = createMockInterviewsSeed();
