/**
 * Historical Sprint 3A seed data — unused after Sprint 3B Supabase integration.
 * Kept only as a placeholder module so older imports do not break.
 */
export const INITIAL_MOCK_JOBS = [] as const;
