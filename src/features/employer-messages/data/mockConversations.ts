/**
 * Sprint 5A mock conversations — unused at runtime after Sprint 5B.
 */
export function createMockConversationsSeed() {
  return [];
}

export const mockConversationsSeed = createMockConversationsSeed();
