"use client";

/**
 * Thin re-export so SAP Jobs Finder auth pages can import from src/layouts
 * while reusing the existing design-system AuthLayout.
 */
export { AuthLayout } from "@/components/auth/AuthLayout";
